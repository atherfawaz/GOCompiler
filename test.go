func integer: numPrint (integer: num, integer: length) {
   while i > 0 :
   	{
   	    char : a;
   		integer: i, j, first, temp;
   		integer: test;
   		char : testu;

    	a := 'x';
   	    while j > 2 : {

   	        if i > 3: {
   	            if j < 0:{
   	                in >> i;
                    print ("enter number");
                    z := 3 * 5 + 24 - 3 / a + 20 + ( 3 / 4 -2);
   	            }
                x := 2 * 3 + ( 5 /2 - 3);
   	        }
        print ("enter number");
   	    }


   	}
}